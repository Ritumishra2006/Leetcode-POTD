#include <bits/stdc++.h>

using namespace std;

/*
 * Complete the 'solve' function below.
 *
 * The function accepts INTEGER n as parameter.
 */

void solve(int n) {
    for(int i = 1; i <= n; i++) {
        for(int s = 1; s < i; s++) {
            cout << " ";
        }
            for(int j = i; j <= n; j++) {
                cout << j << " ";
            
        }
        cout << endl;
        
    }
    for(int i = n-1; i >= 1; i--) {
        for(int s = 1; s < i; s++) {
            cout << " ";
        }
        
        for(int j = i; j <= n; j++){
            
            cout << j << " ";
        }
        
    cout << endl;
    }

}

int main()
{
   int n;
   cin >> n;
 solve(n);
    return 0;
}
