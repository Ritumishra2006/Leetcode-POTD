class Solution {
public:
    int addDigits(int n) {
        int ans = 0;
      
        while(n > 0) {
            int dig = n%10;
            ans += dig;
            n/=10;
            }
            if(ans < 10) 
            return ans;
         else {
            return addDigits(ans);
         }
    }
};